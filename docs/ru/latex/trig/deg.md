# \deg

```latex
\deg{x}
```

Транслируется в `((x) * M_PI / 180.0)` — перевод из градусов в радианы.

Синоним: `deg(x)`.

## Пример

```mc
f(x) = \deg{x}
// → ((x) * M_PI / 180.0)
```

Смотри [trig: sin](sin.md) · [trig: cos](cos.md)
