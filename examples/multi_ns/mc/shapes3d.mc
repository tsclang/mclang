// 3D shapes — sphere, cube, cylinder

// Sphere
area(r)   = 4 * \pi * r^2
volume(r) = (4 / 3) * \pi * r^3

// Cube
cube_area(a)   = 6 * a^2
cube_volume(a) = a^3

// Cylinder
cyl_area(r, h)   = 2 * \pi * r * (r + h)
cyl_volume(r, h) = \pi * r^2 * h
